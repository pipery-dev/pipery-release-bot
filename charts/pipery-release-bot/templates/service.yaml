apiVersion: v1
kind: Service
metadata:
  name: {{ include "pipery-release-bot.fullname" . }}
  labels:
    {{- include "pipery-release-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pipery-release-bot.selectorLabels" . | nindent 4 }}
